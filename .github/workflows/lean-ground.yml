name: Lean ground soundness PR50
on:
  pull_request:
    paths:
      - 'formal/ground/**'
      - 'research/ground_query/**'
      - 'research/regression/test_ground_formal.py'
      - '.github/workflows/lean-ground.yml'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  ground-soundness:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      PYTHONDONTWRITEBYTECODE: '1'
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065
        with:
          python-version: '3.12'
      - name: Audit release metadata and prepare identified research tree
        run: python .github/scripts/release_compatibility.py --checkout --output reproduction/lean_ground/release-compatibility.json
      - name: Build pinned Lean package with kernel-checked examples
        uses: leanprover/lean-action@50fcf42d2e460296f1a34b402e990d1b24f8b596
        with:
          lake-package-directory: formal/ground
          use-github-cache: false
      - name: Clean rebuild, exact axiom audit and negative integrity controls
        run: |
          python -O -m formal.ground.check_ci reproduction/lean_ground/checks --require-clean
          git archive HEAD | gzip -n > reproduction/lean_ground/source.tar.gz
      - uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        if: always()
        with:
          name: lean-ground-pr50
          path: reproduction/lean_ground/
          retention-days: 30
          if-no-files-found: error
