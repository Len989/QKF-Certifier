"""Render compact Russian handoff documents from audited measurements."""
import json
from environment import ROOT


def short(s):return s.removeprefix('KnownBits_')


if __name__=='__main__':
    a=json.loads((ROOT/'audit/AUDIT.json').read_text());assert a['status']=='passed'
    m=a['modes'];tests=a['mechanism_tests']
    labels={'baseline':'Предыдущий слой','guards':'Новые строки, без новых фактов контракта','context':'Новые строки + проверенный контекст'}
    table='| Режим | Целые программы | Компоненты | Подготовки в старой грамматике |\n| --- | ---: | ---: | ---: |\n'
    for mode,r in m.items():table+=f"| {labels[mode]} | {r['whole_count']}/39 | {r['component_proofs']}/411 | {r['prepared_supported']}/411 |\n"
    whole=', '.join(short(x) for x in m['context']['whole'])
    report=f'''# QKF: проверяемый контекст и наблюдаемое действие

Итог: **9/39 полных исходных программ и 87/411 доказанных компонентов**.
UaddSat закрыт целиком, 14/14. Add вырос с 4/13 до 8/13. UsubSat
остаётся 4/16. Результат относится ко всем положительным ширинам на
неизменённых целевых спецификациях текущего корпуса.

{table}
Полностью закрыты: {whole}.

Компоненты проверяются до первой остановки на каждой программе. Число
87 не является исчерпывающей оценкой всех независимо доказуемых компонентов.
226 подготовок — выражения, принятые старой битовой грамматикой, а не 226
доказательств целевых свойств. Полнота программы включает все её компоненты,
исходную meet-композицию и отдельную проверку w=1.

## Что изменилось по существу

В вывод наблюдений введено проверяемое происхождение предпосылок:
фактические условия ветви, исходные маски входов и перенос равенств.
Контракт не выдаётся произвольному выражению по флагу producer: внешний
checker заново строит привязку к конкретным исходникам и аргументам.

Один носитель «ноль или одиночный бит» теперь получается как из условной
связи предшественников, так и из ранее доказанной границы пробега. Его
используют разные потребители: счётчики и остаток. Именованные значения
исходного select также могут образовать строку, даже если это произвольные
слова, а не малые константы.

Самое перспективное изменение — наблюдение **действия на данное слово**.
Для одного сдвига Add счётчик не восстановлен и может доходить до w.
Вынужденное ограничение сдвигаемого слова всё же оставляет два эффекта:
тождественное действие или ноль. Это продолжает направление общей теории
наблюдений и факторов. Имён программ в новом поиске и проверке нет.

На реальном корпусе выбраны всего пять новых переписываний: две строки
исходного select, один остаток по разреженному делителю, одно наблюдение
действия на ограниченное слово и одно условное наблюдение счётчика.
Последние два требуют фактов исходного контракта. Контроль с их отключением
в новом слое даёт 81 компонент; полный режим — 87.

## Что проверено

- Перед общим прогоном зафиксированы протокол и набор из {a['freeze']['frozen_files']} файла.
  Все {a['freeze']['baseline_files']} унаследованных файла остались неизменными.
- Все 39 программ повторены в каждом из трёх режимов. Все 117 процессов
  replay проверили подготовки и доступные доказательства с заблокированным
  поиском и импортом SMT-решателей.
- Контроль повторил прежние 8/39, 79/411, все 411 конечных выражений
  по их каноническим JSON-хешам и исходные статусы компонентов.
- {tests['representations']} записей в {tests['families']} семействах прошли
  проверку условного наблюдения и replay; {tests['representation_checks']:,}
  конечных/случайных сравнений. Удаление каждой из двух предпосылок дало
  {tests['removed_premises_refused']} ожидаемых отказа.
- Ещё {tests['consumer_cases']} случая переноса между потребителями дали
  {tests['consumer_checks']:,} сравнений. Проверены {tests['corruptions_refused']}
  повреждённых/необоснованных сертификатов и {tests['separating_inputs']}
  сохранённых конкретных разделяющих входов.

Конечные проверки дополняют доказанные нативные мосты и replay; они не
подменяют обоснование всех ширин. Эти тесты составлены при разработке,
а корпус уже был известен. Независимого внешнего holdout в этом этапе нет.

Сумма времени producer/replay в этом прогоне: предыдущий слой
{m['baseline']['producer_seconds']+m['baseline']['replay_seconds']:.2f} с,
полный новый режим {m['context']['producer_seconds']+m['context']['replay_seconds']:.2f} с.
Это единичное локальное измерение, не утверждение о статистически надёжном
ускорении. Общий битовый автомат, цели и ресурсные бюджеты прежние.

## Где остаются трудности

Add остановился на partial_solution_8 с urem по маске старших битов;
partial_solution_12 также содержит неподдержанный зависимый сдвиг.
UsubSat остановился на partial_solution_4 с sdiv, хотя далее есть ещё
несколько неподдержанных форм. Новый слой не притворяется общим решением
произвольных сдвигов, деления или восстановления слов.

23 программы не имеют адаптера целевой спецификации. Пять старых
безусловно усиленных целей опровергнуты; это не означает дефект корректных
исходных LLVM-контрактов. Подробные статусы всех 39 — в COVERAGE_RU.md.

## Оценка и направление

Это содержательное улучшение механизма вывода наблюдений: контекст стал
проверяемым ресурсом, и одно свойство обслуживает разные операции. Базовый
автомат при этом не перестраивался. Пока нельзя объявлять результат общей
заменой SMT: доверенные семантические мосты ещё добавляются вручную,
охват ограничен, общего минимального фактора программы нет.

Следующий приоритет — запрос конкретного наблюдения результата. Для
UsubSat сначала нужен только знак частного; для Add — действие делителя,
который равен нулю или имеет установленный старший бит. Кандидаты и их
граничные случаи записаны в 05_NEXT_STEPS_RU.md. После одного такого цикла
имеет смысл перенос на новый замороженный внешний набор, не ожидая 39/39.

Подробное обоснование — THEORY_RU.md. Полные исходники, результаты,
остановки, обе статьи и точный предыдущий архив включены в пакет.
'''.replace('264,576','264 576').replace('119,115','119 115')
    (ROOT/'REPORT_RU.md').write_text(report)
    lines=['# Матрица всех 39 программ','',
        'Количество компонентов — до первой остановки. Знак ✓ означает полную исходную программу для всех положительных ширин.','',
        '| Программа | Предыдущий слой | Только новые строки | С контекстом | Подготовлено в грамматике | Остановка / статус |',
        '| --- | ---: | ---: | ---: | ---: | --- |']
    status={'unsupported_target':'нет адаптера цели','explicit_target_refuted':'старая усиленная цель опровергнута','certificate_produced':'полностью доказана'}
    for row in a['matrix']:
        values=[]
        for mode in ['baseline','guards','context']:
            r=row[mode];values.append(f"{r['proved']}/{r['components']}"+(' ✓' if r['whole'] else ''))
        c=row['context'];reason=status.get(c['status'],str(c['stopped_at'])+': '+', '.join(c['stopped_blockers']))
        lines.append(f"| {short(row['case'])} | {' | '.join(values)} | {c['supported']}/{c['components']} | {reason} |")
    lines += ['', '23 отсутствующих адаптера и пять контрпримеров к усиленным целям не менялись в этом этапе.',
              'Нельзя трактовать неподдержанную подготовку как опровержение, а подготовку в грамматике — как доказательство.','']
    (ROOT/'COVERAGE_RU.md').write_text('\n'.join(lines))
    start='''# QKF — начни здесь

Актуальный этап: **проверяемые условные наблюдения**, 2026-09-13.
Автор проекта — Леонид Щербаков. Сохранять идеологию и ограничения из
PROJECT_PRINCIPLES_RU.md; сначала искать решения через наблюдения,
вынужденные связи, носители и факторы первых двух статей.

Состояние: **9/39 полных программ, 87/411 компонентов, 226/411 подготовок**.
Полные программы: And, Or, Xor, Smax, Smin, Umax, Umin, Sub, UaddSat.
UaddSat 14/14, Add 8/13, UsubSat 4/16. Add остановился на компоненте 8,
UsubSat на компоненте 4. Компоненты считаются до первой остановки.

## Порядок восстановления

1. REPORT_RU.md — результат, ограничения и значение изменения.
2. THEORY_RU.md — происхождение предпосылок, носитель и новые строки.
3. COVERAGE_RU.md и audit/AUDIT.json — точные знаменатели и все остановки.
4. 05_NEXT_STEPS_RU.md — следующий согласованный приоритет.
5. papers/paper_I и papers/paper_II — обе полные статьи и исходники.

Реализация нового слоя: context_kernel.py, context_producer.py,
context_stage_kernel.py, context_stage_producer.py. Три режима: baseline,
guards, context. Основной автомат и старые файлы не изменены. Никаких
правил по имени конкретной программы в новом слое нет.

FREEZE.json фиксирует код, данные разработки и протокол до итогового
прогона. results/{baseline,guards,context} содержит все 39 подготовок,
доступные доказательства, остановки и отдельные результаты replay.
validation/frozen_representations и validation/frozen_consumers — итоговые
тесты механизма; development — сохранённые промежуточные опыты.
Разработочные прототипные сертификаты не объявляются сертификатами
финальной версии; для текущего вывода использовать results/context.

## Команды

Из каталога этого файла, Python 3.12 и стандартная библиотека:

    python restore_data.py
    python freeze_context.py
    python audit_context.py

Полное повторение в отдельном каталоге, без перезаписи результатов:

    python reproduce_context.py

Уже выполненные измерения повторять ради восстановления состояния не
нужно. Следующий код разрабатывать отдельным этапом, не меняя freeze.

Большие JSON-результаты и разработочные опыты упакованы без потерь в
EXPERIMENT_DATA.tar.xz. restore_data.py проверяет DATA_MANIFEST.json и
восстанавливает их исходные пути, отказываясь перезаписывать отличающиеся
файлы. Код, документы и статьи лежат непосредственно в ZIP.

## История и целостность

prior_stage/QKF_BOUNDARY_ACTIONS_2026-09-13.zip — точный предыдущий полный
архив (SHA256 a231c3fa18bde0c28149cb098d6006f0b29ee186e9a2c37b6310ed9de1245884).
В нём вся более ранняя история, внешние сравнения, контракты и обе статьи.
Текущие статьи продублированы рядом для удобства восстановления.
PACKAGE_MANIFEST.json содержит размеры и SHA256 файлов текущего пакета.

Новые исследования — на уже известном корпусе. Прежние 11/11 или новые
числа тестов механизма не превращать в независимый внешний benchmark.
Не смешивать полные программы, компоненты и принятые подготовки.
'''
    (ROOT/'00_START_HERE_RU.md').write_text(start)
    (ROOT/'DEVELOPMENT_HISTORY_RU.md').write_text('''# Ход разработки до freeze

Исходная контрольная точка: boundary-actions-v2, 8/39 и 79/411.
Новый каталог создан отдельно, унаследованные файлы сохранены без изменений.

- uadd_v1: ошибка восстановления пути к унаследованному low_boundary_lemma.json;
  полный traceback сохранён. После переноса зависимости uadd_v2 дал 14/14.
- add_v1: прежняя остановка на компоненте 4. Добавлены общие source-select
  строки и перенос существующей разреженной границы в остаток.
- add_v2: 6/13, остановка на компоненте 6. Добавлено наблюдение действия
  на слово с вынужденной поддержкой; add_v3 дал 8/13.
- uadd_v3: 14/14 повторно на окончательном коде. UsubSat остался 4/16.
- Первый запуск локального harness исправлен с valid_masks на фактическое
  имя kbwords в унаследованном oracle. Сам oracle не менялся.
- validation/development_v2 и consumers_v1 пройдены до freeze.
- После FREEZE.json выполнены три полных режима и два фиксированных набора
  проверки. Замороженный код не изменялся. audit/report/package-скрипты
  добавлены после опыта и служат только учёту, воспроизведению и передаче.

Ранние прототипные результаты сохранены как данные разработки. Окончательные
сертификаты и числа находятся только в results и audit/AUDIT.json.
''')
    print('Wrote REPORT_RU.md, COVERAGE_RU.md, 00_START_HERE_RU.md, DEVELOPMENT_HISTORY_RU.md')
