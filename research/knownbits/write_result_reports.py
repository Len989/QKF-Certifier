"""Render project handoff from audited final evidence."""
import json
from pathlib import Path
ROOT=Path(__file__).resolve().parent


def write(n,s):(ROOT/n).write_text(s.strip()+'\n')
def short(n):return n.removeprefix('KnownBits_')


if __name__=='__main__':
    a=json.loads((ROOT/'audit/AUDIT.json').read_text());assert a['status']=='passed'
    b,s,o=[a['modes'][m] for m in ['baseline','sign','observed']]
    matrix={r['case']:r for r in a['matrix']}
    whole=', '.join(short(n) for n in o['whole'])
    tests=a['mechanism_tests']
    table='| Режим | Целые программы | Доказанные компоненты | Подготовки в поддержанной грамматике |\n| --- | ---: | ---: | ---: |\n'
    for label,m in [('Предыдущий context',b),('Добавлено наблюдение знака',s),('Полный новый слой',o)]:
        table+=f"| {label} | {m['whole_count']}/39 | {m['component_proofs']}/411 | {m['prepared_supported']}/411 |\n"
    write('REPORT_RU.md',f'''
# QKF: результат этапа наблюдений результата

**Полностью доказано {o['whole_count']}/39 программ, {o['component_proofs']}/411 компонентов.**
Add закрыт целиком: 8/13 → 13/13. UsubSat: 4/16 → 16/16.
Полные сертификаты относятся к исходным программам и всем
положительным ширинам в прежнем явно указанном контракте.

{table}

Полностью закрыты: {whole}.

Компоненты доказываются до первой остановки в каждой программе; число
{o['component_proofs']} не является максимальным числом независимо доказуемых компонентов.
Поддержанные подготовки означают только допустимость их формы для
прежней грамматики. Полная программа требует всех компонентов, исходной
meet-композиции и отдельной проверки оригинала при w=1.
Компонентные преобразования действуют при w≥2; для целого результата
недостающая ширина проверяется на исходной программе.

## Что изменилось

Теперь потребитель явно запрашивает нужное наблюдение результата. Например,
для знаковой маски достаточно вычислить знак частного. Этот вывод
передаётся через битовые операции, условия и сдвиги. Одно наблюдение
обслуживает маску, предикат, выделение бита и заполнение знаком.

Добавлен проверяемый носитель делителя «ноль или установлен старший бит»:
он оставляет три клетки unsigned-деления и остатка. Для выровненного
сдвига связь входных масок позволяет доказать нулевой старший бит без
восстановления счётчика. Исходные select также задают строки действий
сдвигов/масок на именованное слово; модульные количества в их клетках
проверяются прежним параметрическим мостом.

В Add остаток по большому делителю снимает остановку partial_solution_8,
а наблюдение выровненного сдвига — последнюю partial_solution_12.
В UsubSat знак частного снимает partial_solution_4, строки действий —
7, 10, 13, элементарный запрос целого слова — вложенные остатки в 12.
Имена программ используются только в учёте и тестах, не в новых правилах.

На всём корпусе полный режим выбрал {sum(o['new_rewrites'].values())} новых переписываний:
строк действий — {o['new_rewrites'].get('action-on-word',0)}, действий деления —
{o['new_rewrites'].get('division-action',0)}, наблюдений знака — {o['new_rewrites'].get('sign',0)},
элементарных запросов слова — {o['new_rewrites'].get('word',0)}.
Режим с одним наблюдением знака даёт {s['component_proofs']} компонентов;
остальные наблюдения необходимы для полного результата.

## Проверка и обнаруженная ошибка

Первый frozen-прогон получил оба новых полных сертификата, но в подготовке
AvgCeilU обнаружена техническая ошибка: shl(1,w−1) превращается в маску
знака, а поиск обращался к несуществующему аргументу количества. Первый
код, все результаты и ошибка сохранены в results; там только 116 replay.

Исправление проверяет тип нормализованного узла. Оно находится в отдельных
файлах v2 и не меняет проверяющее ядро. После второй фиксации повторены
все три режима, каждый с 39 свежими producer и replay. Все 117 итоговых
replay прошли с заблокированным поиском и импортом SMT-решателей. Все
{a['first_frozen_run']['unchanged_available_final_expressions']} доступных конечных выражений первого прогона совпали со вторым.

Первый freeze связывает 305 файлов, второй — {a['freeze']['frozen_files']}.
Все 262 унаследованных файла неизменны. Контроль повторил прежние статусы,
9/39, 87/411 и все 411 конечных выражений. Исходники, цели, контракты и
ресурсные бюджеты сохранены; регрессий доказанного покрытия нет.

Проверки механизма после второго freeze:

- {tests['cases']} случаев, {tests['representation_checks']} сравнений разных записей и входов;
- {tests['exhaustive_signed_division_checks']} пар для знака signed division, ширины 2–8;
- {tests['exhaustive_high_divisor_checks']} пар для большого/нулевого unsigned-делителя, ширины 2–8;
- {tests['contextual_checks']} допустимых входов действительного контекста Add, ширины 2–4;
- {tests['corruptions_rejected']} отклонённых повреждений и удалений предпосылок;
- {tests['carrier_refusals']} необоснованных носителя, {tests['separating_inputs']} разделяющих входов;
- {tests['producer_normalization_cases']} случаев нормализации, включая воспроизведение прежнего падения.

Конечные проверки проверяют реализацию; все ширины обосновываются
параметрическими мостами и replay, с отдельным w=1. Корпус известен,
а тесты построены при разработке. Независимого внешнего испытания на
этом этапе нет.

Суммы времени producer/replay в последовательном итоговом прогоне:
baseline {b['producer_seconds']+b['replay_seconds']:.2f} с,
sign {s['producer_seconds']+s['replay_seconds']:.2f} с,
observed {o['producer_seconds']+o['replay_seconds']:.2f} с.
Это один локальный запуск; из него не следует статистически надёжное
ускорение. Сравнение с современными решателями здесь не проводилось.

## Реалистичная оценка

Получено существенное расширение механизма наблюдений: он работает с
нужной частью результата и переносит её между потребителями. Оно дало
два полных исходных доказательства, а не только локальные упрощения.
Базовый автомат при этом не перестраивался.

Теперь закрыты все 11 программ, для которых уже имелась поддержанная
неопровергнутая целевая спецификация. Общий знаменатель остаётся 39:
23 цели ещё не имеют адаптера, а пять старых безусловно усиленных целей
опровергнуты. Эти пять результатов не доказывают ошибку исходных LLVM
контрактов. Все строки и остановки приведены в COVERAGE_RU.md.

Это удачная точка для проверки переноса. Следует зафиксировать текущий
слой и испытать его на новом внешнем наборе без подстройки по результатам.
Работу над 23 целями стоит начинать с точного описания операции и
допустимости входов. Добавление адаптера само по себе не считается
алгоритмическим успехом.

Глобальная цель — механизм, который сам строит достаточное наблюдение,
предъявляет проверяемую склейку и объясняет, какого различия ему не хватило.
В этой версии запрос уже явный, но семантические мосты ещё написаны вручную,
а минимальность произвольного фактора и общий поиск насыщения не реализованы.
Подробные доказательства и связь с обеими статьями — THEORY_RU.md.
Следующий исследовательский цикл — 05_NEXT_STEPS_RU.md.
''')
    coverage='''# Покрытие всех 39 программ

Итоговый второй frozen-прогон. «Подг.» — количество компонентов без
неподдержанных операций в подготовленном выражении, не доказательства.
Компоненты доказываются последовательно до первой остановки.

| Программа | Всего компонентов | Baseline: доказано | Sign: доказано | Observed: доказано | Подг. observed | Итог |
| --- | ---: | ---: | ---: | ---: | ---: | --- |
'''
    for row in a['matrix']:
        x=row['observed'];status=x['status']
        label={'certificate_produced':'полностью доказана','unsupported_target':'нет адаптера цели',
               'explicit_target_refuted':'усиленная цель опровергнута'}.get(status,status)
        coverage+=f"| {short(row['case'])} | {x['components']} | {row['baseline']['proved']} | {row['sign']['proved']} | {x['proved']} | {x['supported']} | {label} |\n"
    coverage+='''
Пять усиленных целей не моделируют исходные флаги допустимости, поэтому
их опровержение не переносится на настоящий контракт операции. Для 23
программ отсутствие цели сохраняется независимо от качества подготовки.
На прежних одиннадцати поддержанных и неопровергнутых целях теперь нет
незакрытых программ. Сырые статусы, свидетельства и все подготовки —
results_v2; первый прогон с технической ошибкой — results.
'''
    write('COVERAGE_RU.md',coverage)
    write('00_START_HERE_RU.md',f'''
# Продолжение QKF: наблюдения результата, 13 сентября 2026

Автор проекта — Леонид Щербаков. Текущий итог: **{o['whole_count']}/39 целых
программ, {o['component_proofs']}/411 доказанных компонентов**, все положительные ширины.
Add 13/13 и UsubSat 16/16 закрыты полностью. Контроль: 9/39, 87/411.
Покрытие всех 39 подробно записано в COVERAGE_RU.md.

## Восстановить текущий проект

После распаковки этого ZIP перейти в его каталог и выполнить:

```bash
python restore_project.py
python freeze_result_v2.py
python audit_result_v2.py
```

Первая команда восстанавливает точные неизменённые исходники и обе статьи
из включённого предыдущего архива, затем все результаты, тесты и неудачи
из EXPERIMENT_DATA.tar.xz. Все байты проверяются; различающиеся локальные
файлы не перезаписываются. Нужен только Python 3.12 и стандартная библиотека,
сеть и другие архивы не требуются.

## Читать в этом порядке

1. PROJECT_PRINCIPLES_RU.md — сохраняемый приоритет общей теории.
2. REPORT_RU.md — результат, сравнение трёх режимов и ограничения.
3. THEORY_RU.md — новые запросы и обоснование семантических мостов.
4. COVERAGE_RU.md — точные знаменатели и все 39 статусов.
5. 05_NEXT_STEPS_RU.md — что делать дальше.

Первые две статьи полностью сохранены в papers/paper_I и papers/paper_II:
исходники, PDF, карты утверждений и собственные проверки. Их текст не менялся.

## Действующие точки входа

- result_kernel.py и result_stage_kernel.py — проверка наблюдений и
  доказательство исходной программы.
- result_v2_producer.py и result_v2_stage_producer.py — исправленный поиск.
- result_v2_run.py — свежие процессы и строгая блокировка поиска в replay.
- validate_result.py — 304 семантических случая и отрицательные проверки.
- validate_result_v2.py — шесть случаев технической регрессии producer.
- audit_result_v2.py — аудит итогового results_v2.

Повтор одного случая в новом каталоге:

```bash
python result_v2_run.py --mode observed --case KnownBits_Add --output reproduction/add
```

Все режимы можно повторить result_v2_run.py, указав baseline, sign,
observed и разные новые каталоги. Итоговые файлы защищены от перезаписи.
run_result_v2_experiment.py относится к первоначальному запуску; его
results_v2 уже заполнен. При повторе нельзя затирать сохранённый эксперимент.

## Что не потерять

FREEZE.json сохраняет первый код и 305 файлов. results содержит первый
прогон с IndexError в подготовке AvgCeilU. Исправление проверяет вид
нормализованного узла перед чтением количества; checker не менялся.
FREEZE_V2.json связывает {a['freeze']['frozen_files']} файлов, включая первый отказ.
results_v2 — итоговый повтор всех трёх режимов с 117 успешными replay.

В audit/AUDIT.json — все суммы и матрица. DATA_MANIFEST.json связывает
каждый файл экспериментальных данных. INHERITED_FILES.json задаёт точное
восстановление неизменённых исходников. PACKAGE_MANIFEST.json описывает
непосредственные файлы пакета. Архив предыдущего этапа сохранён без
изменений в prior_stage/QKF_CONTEXT_OBSERVATIONS_2026-09-13.zip;
он содержит всю более раннюю историю.

Не называть этот корпус новым holdout; не превращать {o['prepared_supported']}
подготовок в целевые доказательства; не объявлять 11/11 вместо 11/39.
Не искать решение по имени программы. Сначала искать нужное наблюдение,
вынужденные клетки и допустимый фактор из общей теории первых двух статей.
''')
    write('05_NEXT_STEPS_RU.md','''
# Следующий цикл: перенос и точная постановка новых целей

Текущий цикл завершён: Add и UsubSat полностью закрыты. Явный запрос
наблюдения результата реализован и испытан. Продолжать подгонку под те же
одиннадцать программ сейчас менее полезно, чем проверить перенос.

## 1. Приоритет: независимый внешний тест текущей версии

Зафиксировать result_v2 и подобрать новый корпус из источника, не
использованного для разработки этих мостов. Сначала зафиксировать весь
перечень, версии исходников, правила отбора, контракты и бюджеты; затем
запускать. После знакомства с результатами не объявлять доработанную
версию прошедшей исходный holdout — для неё нужен следующий набор.

Начать с задач, где корректно задаётся тот же тип утверждений: свойства
битовых результатов, известные биты и допустимые входы. Отдельно учитывать
неподдержанный синтаксис, отсутствие семантической цели, нехватку наблюдения,
исчерпание ресурса и опровержение. Считать все отобранные задачи, включая
ошибки и остановки. Специально построенные вариации наших форм оставить
регрессионными тестами, не выдавая их за внешний корпус.

Сравнение с общепринятыми решателями имеет смысл после выравнивания задачи:
одни и те же формулы и предпосылки, одинаковые лимиты и проверка свидетельств.
Числа для фиксированных ширин и параметрические доказательства всех ширин
показывать отдельно. Зафиксировать реальные версии решателей перед новым
экспериментом; текущий пакет не содержит нового сравнительного результата.

Критерий успеха — перенос наблюдений без ручных правил по задачам, новые
полные сертификаты и понятные причины отказа. Одно ускорение на знакомой
формуле не закрывает этот критерий.

## 2. Расширить язык целевых спецификаций

23 программы пока не имеют адаптера цели. Это отдельный барьер постановки,
а не 23 доказанных предела метода. Следует описывать операцию и допустимость
входов явным проверяемым дескриптором: его привязка к исходнику не должна
зависеть только от названия файла. Метаданные синтезированных компонентов
также требуют проверки и сами по себе не определяют правильную цель.

Первым небольшим кандидатом является unsigned absolute difference, после
проверки его точного контракта: выбор между A−B и B−A уже выражается
поддержанными операциями. Затем разумно рассмотреть четыре средних с
направлением округления; для них нужны точные переносы, знак и потерянный
младший бит. Не добавлять сразу общий target для произвольного умножения,
деления и сдвигов и не обещать 39/39.

Пять опровергнутых усилений Add/Sub нужно оформить как настоящие условные
задачи с nsw/nuw-предпосылками. Прежние контрпримеры сохранить: они должны
объяснять, почему без этих предпосылок цель ложна. Исправление спецификации
не считать само по себе улучшением алгоритма.

## 3. Исследовательский приоритет внутри теории

Следующая общая проблема — совместные наблюдения. Сейчас отдельно запрошены
знак, свойство носителя или действие. Когда потребителю нужны одновременно
знак, нулевость и выбранный бит, строить защищённый совместный носитель,
сохранять совместимость его клеток и проверять, какие различия вынуждены
реальным контекстом. Независимое перемножение ответов может добавить
невозможные клетки и потерять полезные связи.

Начать с одной реальной новой остановки внешнего набора. Предъявить
полную строку наблюдаемого действия и разделяющий пример для более слабого
интерфейса. Затем попытаться выводить достаточный фактор через насыщение
отношений из статей, сохраняя проверяемое происхождение каждого равенства.
Не называть фактор минимальным без соответствующего доказательства.

Долгосрочная цель сохраняется: существенная замена механизмов SMT через
автоматически выводимые достаточные наблюдения и проверяемую склейку.
Ближайший измеримый рубеж — перенос на незнакомые задачи при сокращении
ручных семантических мостов. Именно это покажет, развивается ли общий
метод, а не только коллекция удачных преобразований.
''')
    print(json.dumps(dict(status='written',reports=4,whole=o['whole_count'],components=o['component_proofs'])))
